/**
 * Global configuration file for the AgroConnect frontend
 */

// Backend API URL
export const API_URL = import.meta.env.VITE_BACKEND_DOMAIN || 'http://localhost:8000';

// Image URLs
export const FALLBACK_IMAGE_URL = 'https://via.placeholder.com/400x200?text=Image+Not+Available';

// Other configuration constants can be added here
